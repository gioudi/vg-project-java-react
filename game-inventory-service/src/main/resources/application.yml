server:
  port: 8082

spring:
  application:
    name: game-inventory-service
  datasource:
    url: jdbc:postgresql://localhost:5434/crud_db
    username: user_crud
    password: password123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:

  instance:
    prefer-ip-address: true
  client:
    http-client-adapter: rest-template
    service-url:
      defaultZone: http://localhost:8761/eureka/